# Skip ALL model files for railway up — Cloudflare rejects uploads > 100MB
# Models are built into Docker layers and persist across redeploys.
# The Dockerfile's COPY app /app/app will use whatever is in the build context.
# For fresh deploys, models must be uploaded separately.
app/model/*.pt
app/model/*.pt.part_*
app/model/*.pth
app/model/*.ckpt
app/model/public/
app/model/videomae_*/
external/
notebooks/
tests/
*.md
*.mp4
*.zip
.git/
__pycache__/
